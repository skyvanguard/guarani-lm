# Quick patch: add python3.10-dev (needed by Triton JIT compile)
FROM guarani-lm:v3

RUN apt-get update && apt-get install -y --no-install-recommends \
        python3.10-dev \
    && rm -rf /var/lib/apt/lists/*
